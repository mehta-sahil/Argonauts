# Layer 2 (local only — the authorize/mitigation Lambdas stay dependency-free).
# Install: python -m pip install -r requirements-ml.txt
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3
shap>=0.44
flask>=3.0          # sim/mock_gateway.py HTTP mode only
matplotlib>=3.7     # evaluation notebook
boto3>=1.34         # scoring_lambda.py local runs
